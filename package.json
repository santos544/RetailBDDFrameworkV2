{
  "name": "retailbddframeworkv2",
  "version": "1.0.0",
  "description": "Playwright TypeScript Cucumber Retail Framework",
  "scripts": {
    "test": "cucumber-js",
    "smoke": "cucumber-js --tags @smoke",
    "regression": "cucumber-js --tags @regression",
    "parallel": "cucumber-js --parallel 3",
    "rerun": "cucumber-js --retry 2"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^10.0.0",
    "@playwright/test": "^1.52.0",
    "multiple-cucumber-html-reporter": "^3.10.0",
    "playwright": "^1.52.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
